# Generate synthetic image from STL with Blender

You need to install Blender and needed Python packages.

The scripts are tested on Ubuntu 24. Blender is installed from snap.

Execute to run the script headless: `blender --background --python generate.py`.
